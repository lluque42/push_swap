********************************************************************************
************************ NEXT COMMIT DESCRIPTION *******************************
********************************************************************************

The print() function for printing the stacks for debugging was improved
to print the elements sorting metadata.

A member left_to_pre_sort member was added to main t_ps_stacks struct.

A new functions collection for a more complex list node data type was
written. This collection replaces the obsolete ps_dlclst_int_utils. In
addition to re-writing the previous functions, the following were added
to get element members and to obtain an integer array from the element
list:
get_cost_a()
get_cost_b()
get_pos_when_sorted()
get_value()
list_to_array()

In the sort collection, the following functions were added:
classic_sorting(): to sort the integer array
set_pos_when_sorted(): to use the sorted integer array to initialize
the pos_when_sorted members of the elements in the list.



********************************************************************************
********************************************************************************
********************************************************************************



********************************************************************************
******************************** TODO ******************************************
********************************************************************************

+I noticed the arguments to ./push_swap from the command line could be either one
(requiring ft_splitting them, because they come from an enviroment variable) or
several (because they come from a direct user keyboard input when running the
program). FIX THIS!	
	+ Confirm, now I can't replicate what I (think I) saw at Campus with Serena?
	and Matías.
		+ Zerisenay's code is so weird in handling the argvs that may be it
		actually needs to be foreseen. First he joins and then he splits (?)

********************************************************************************

+ Every error message to stderr.
+ Test if any argv is ""
+ Test for 0 value (atoi returns 0 in other cases additionally to "0").
+ Test for out of range integers

********************************************************************************
********************************************************************************
********************************************************************************






********************************************************************************
******************************** DONE ******************************************
********************************************************************************

+ Remove the redundant printing to stdout when calling ss, rr, rrr.

********************************************************************************
********************************************************************************
********************************************************************************
